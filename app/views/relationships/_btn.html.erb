<% if current_user.following?(user) %>
  <%= link_to 'following', user_relationships_path(user.id), method: :delete, class:"btn btn-success btn-sm" %>
<% else %>
  <%= link_to 'follow', user_relationships_path(user.id), method: :post, class:"btn btn-outline-success btn-sm" %>
<% end %>